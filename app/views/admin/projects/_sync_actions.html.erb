<%= button_to 'Sync Project', sync_admin_project_path(resource) unless resource.processing? %>

<% if resource.state.in?(Project::SUCCESS_STATES) %>
  <%= button_to 'Download import file', download_import_admin_project_path(resource), method: :get %>
<% end %>
